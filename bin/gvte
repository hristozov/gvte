#!/usr/bin/env ruby
$LOAD_PATH.unshift File.dirname(__FILE__) + '/../lib'
require 'gvte'

module Gvte
  Gtk.init
  win = MainWindow.new(OptionsParser::parse_argv ARGV).set_default_size(400, 400).set_resizable(true).set_decorated(true).show_all
  Gtk.main
end

